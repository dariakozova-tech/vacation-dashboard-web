---
phase: 02-schema-services-and-actions
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/utils/vacationLogic.ts
  - src/lib/utils/vacationLogic.test.ts
  - src/lib/services/employees.ts
  - src/lib/services/vacationRecords.ts
autonomous: true
requirements:
  - SVC-01
  - SVC-02
  - SVC-03
  - SVC-04

must_haves:
  truths:
    - "vacationLogic.ts is a pure TypeScript ES module with all functions from the Electron vacationLogic.js"
    - "All vacationLogic unit tests pass"
    - "getEmployees() returns active employees ordered by name"
    - "getVacationRecords(id) and getAllVacationRecords() return typed rows"
  artifacts:
    - path: "src/lib/utils/vacationLogic.ts"
      provides: "Pure TypeScript port of vacation business logic"
      exports: ["calculateEarnedDays", "calculateUsedDays", "calculatePeriodDays", "calculateEmployeeBalance", "RESET_DATE", "DAYS_PER_MONTH"]
    - path: "src/lib/utils/vacationLogic.test.ts"
      provides: "Unit tests for all vacationLogic exported functions"
    - path: "src/lib/services/employees.ts"
      provides: "getEmployees(), addEmployee(), updateEmployee(), deleteEmployee() service functions"
    - path: "src/lib/services/vacationRecords.ts"
      provides: "getVacationRecords(), getAllVacationRecords(), addVacationRecord(), updateVacationRecord(), deleteVacationRecord() service functions"
  key_links:
    - from: "src/lib/services/employees.ts"
      to: "src/lib/db/index.ts"
      via: "import { db } from '@/lib/db'"
      pattern: "from '@/lib/db'"
    - from: "src/lib/services/employees.ts"
      to: "src/lib/db/schema.ts"
      via: "import { employees } from '@/lib/db/schema'"
      pattern: "from '@/lib/db/schema'"
    - from: "src/lib/services/vacationRecords.ts"
      to: "src/lib/db/schema.ts"
      via: "import { vacationRecords } from '@/lib/db/schema'"
      pattern: "from '@/lib/db/schema'"
    - from: "src/lib/utils/vacationLogic.ts"
      to: "date-fns"
      via: "import { parseISO, ... } from 'date-fns'"
      pattern: "from 'date-fns'"
---

<objective>
Port the Electron vacationLogic.js to TypeScript, write full unit tests, and implement server-side service functions for employees and vacation records.

Purpose: Establishes the business logic layer and data access functions that Server Actions will wrap in plan 03 and 06.
Output: vacationLogic.ts (pure), vacationLogic.test.ts (full), services/employees.ts, services/vacationRecords.ts.
</objective>

<execution_context>
@/Users/dariakozova/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dariakozova/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-schema-services-and-actions/02-RESEARCH.md
@.planning/phases/02-schema-services-and-actions/02-01-SUMMARY.md

<interfaces>
<!-- Schema types created in plan 01 -->
From src/lib/db/schema.ts (created in plan 01):
```typescript
export type Employee = typeof employees.$inferSelect;
// { id: number; fullName: string; hireDate: string; isActive: boolean; isDeel: boolean; email: string | null; createdAt: Date }

export type VacationRecord = typeof vacationRecords.$inferSelect;
// { id: number; employeeId: number; recordType: 'period' | 'days_sum' | 'balance_reset'; startDate: string | null; endDate: string | null; daysCount: number | null; year: number | null; note: string | null; createdAt: Date }
```

Electron vacationLogic.js exports (to port):
- RESET_DATE = new Date('2026-01-01T00:00:00')
- DAYS_PER_MONTH = 2
- calculateEarnedDays(hireDate: Date, asOf?: Date): number
- calculateUsedDays(records: VacationRecord[], year: number): number
- calculatePeriodDays(startDate: string, endDate: string): number
- calculateEmployeeBalance(employee, records): { earned, used2024, used2025, used2026, balance, wasReset, resetDays }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port vacationLogic.js to TypeScript and write full unit tests</name>
  <files>src/lib/utils/vacationLogic.ts, src/lib/utils/vacationLogic.test.ts</files>
  <behavior>
    - calculateEarnedDays(hireDate '2021-07-01', asOf '2026-03-12') returns 114 (57 full months × 2)
    - calculatePeriodDays('2024-01-15', '2024-01-20') returns 5 (exclusive end)
    - calculateUsedDays with a 'period' record of 5 days returns 5
    - calculateUsedDays with a 'days_sum' record of daysCount=3 returns 3
    - calculateEmployeeBalance for Бондаренко (hire 2021-07-01, -4 balance before reset) returns wasReset: true
    - calculateEmployeeBalance for an employee with positive balance returns wasReset: false
  </behavior>
  <action>
First install date-fns:
```
npm install date-fns --no-package-lock
```

Read /Users/dariakozova/Documents/vacation-dashboard/src/utils/vacationLogic.js to copy logic verbatim.

Create src/lib/utils/vacationLogic.ts:
- Port all functions 1:1 from the JS source
- Add TypeScript types (use Employee and VacationRecord types from schema.ts, or duplicate inline if cleaner)
- All functions remain pure — no async, no DB access
- Export: RESET_DATE, DAYS_PER_MONTH, calculateEarnedDays, calculateUsedDays, calculatePeriodDays, calculateEmployeeBalance

Replace the scaffold in src/lib/utils/vacationLogic.test.ts with full tests:
- Import all exported functions from vacationLogic.ts
- Test each function with cases from the behavior block above
- Test edge cases: hire date on the 1st vs mid-month, balance_reset record type, wasReset=true scenario
- Use the known seed employees (Бондаренко, Мельник) as test fixtures

TDD cycle: Write tests first (RED), then implement until GREEN.
  </action>
  <verify>
    <automated>npx jest src/lib/utils/vacationLogic.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All vacationLogic unit tests pass; no TypeScript errors; calculateEmployeeBalance matches Electron output for both seed employees.</done>
</task>

<task type="auto">
  <name>Task 2: Implement service functions for employees and vacation records</name>
  <files>src/lib/services/employees.ts, src/lib/services/vacationRecords.ts</files>
  <action>
Create src/lib/services/employees.ts:
- `import 'server-only';`
- Import: db from '@/lib/db', employees from '@/lib/db/schema', eq/asc from 'drizzle-orm'
- getEmployees(): returns active employees (isActive=true), ordered asc by fullName
- addEmployee(data: { fullName, hireDate, isDeel?, email? }): db.insert.returning()
- updateEmployee(data: { id, fullName?, hireDate?, isActive?, isDeel?, email? }): db.update.set(data).where(eq(id)).returning()
- deleteEmployee(id: number): deletes vacation_records first (import vacationRecords from schema), then employees. Use Promise.all only if independent — here they're sequential (delete records, then employee).

Create src/lib/services/vacationRecords.ts:
- `import 'server-only';`
- Import: db, vacationRecords, employees from schema; eq/asc/desc from 'drizzle-orm'
- getVacationRecords(employeeId: number): returns all records for that employee ordered by startDate asc
- getAllVacationRecords(): returns all records ordered by employeeId asc, then startDate asc
- addVacationRecord(data: NewVacationRecord omitting id/createdAt): db.insert.returning()
- updateVacationRecord(data: { id } & Partial<...>): db.update.returning()
- deleteVacationRecord(id: number): db.delete.where(eq(id))

Note: deleteEmployee in employees.ts must import vacationRecords from schema to delete child records first.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Both service files compile without TypeScript errors; all functions exported; server-only guard present in both files.</done>
</task>

</tasks>

<verification>
- `npx jest src/lib/utils/vacationLogic.test.ts` — all tests pass
- `npx tsc --noEmit` — exits 0 across all new files
- services/employees.ts and services/vacationRecords.ts have `import 'server-only'` as first line
- deleteEmployee implements manual cascade (delete records first, then employee)
</verification>

<success_criteria>
- vacationLogic.ts: All 4+ functions exported, pure, no async
- vacationLogic.test.ts: Tests for all exported functions, including wasReset scenario
- services/employees.ts: 4 exported async functions, server-only guarded
- services/vacationRecords.ts: 5 exported async functions, server-only guarded
</success_criteria>

<output>
After completion, create `.planning/phases/02-schema-services-and-actions/02-02-SUMMARY.md`
</output>

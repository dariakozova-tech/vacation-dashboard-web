---
phase: 02-schema-services-and-actions
plan: "06"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/lib/actions/vacationRecords.ts
autonomous: true
requirements:
  - ACT-05
  - ACT-06
  - ACT-07

must_haves:
  truths:
    - "Server Actions for vacation record CRUD exist and compile without errors"
    - "Each action returns a discriminated union { success: true; data } | { success: false; error }"
    - "Mutation actions call revalidatePath('/') after successful DB operation"
  artifacts:
    - path: "src/lib/actions/vacationRecords.ts"
      provides: "Server Actions wrapping vacation record service functions"
      exports: ["getVacationRecordsAction", "getAllVacationRecordsAction", "addVacationRecordAction", "updateVacationRecordAction", "deleteVacationRecordAction"]
  key_links:
    - from: "src/lib/actions/vacationRecords.ts"
      to: "src/lib/services/vacationRecords.ts"
      via: "import { getVacationRecords, getAllVacationRecords, addVacationRecord, updateVacationRecord, deleteVacationRecord } from '@/lib/services/vacationRecords'"
      pattern: "from '@/lib/services/vacationRecords'"
    - from: "src/lib/actions/vacationRecords.ts"
      to: "next/cache"
      via: "import { revalidatePath } from 'next/cache'"
      pattern: "from 'next/cache'"
---

<objective>
Create Server Actions that wrap the vacation record service functions, returning discriminated union results and revalidating cache on mutations.

Purpose: Completes the Server Actions layer for vacation records (ACT-05 through ACT-07), pairing with the employee actions in plan 03 to give Phase 3 UI access to all CRUD operations.
Output: src/lib/actions/vacationRecords.ts with 5 exported Server Actions.
</objective>

<execution_context>
@/Users/dariakozova/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dariakozova/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-schema-services-and-actions/02-RESEARCH.md
@.planning/phases/02-schema-services-and-actions/02-02-SUMMARY.md

<interfaces>
<!-- Service functions from plan 02 -->
From src/lib/services/vacationRecords.ts:
```typescript
export async function getVacationRecords(employeeId: number): Promise<VacationRecord[]>;
export async function getAllVacationRecords(): Promise<VacationRecord[]>;
export async function addVacationRecord(data: NewVacationRecord): Promise<VacationRecord>;
export async function updateVacationRecord(data: { id: number } & Partial<NewVacationRecord>): Promise<VacationRecord>;
export async function deleteVacationRecord(id: number): Promise<void>;
```

ActionResult discriminated union (define locally, same as in employees actions):
```typescript
type ActionResult<T> =
  | { success: true; data: T }
  | { success: false; error: string };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vacation record Server Actions</name>
  <files>src/lib/actions/vacationRecords.ts</files>
  <action>
Create src/lib/actions/vacationRecords.ts:

First line: `'use server';`

Import:
- `import { revalidatePath } from 'next/cache';`
- `import { getVacationRecords, getAllVacationRecords, addVacationRecord, updateVacationRecord, deleteVacationRecord } from '@/lib/services/vacationRecords';`
- Import VacationRecord and NewVacationRecord types from '@/lib/db/schema' for return typing

Define ActionResult<T> type alias locally in this file.

Implement 5 exported async functions:

1. getVacationRecordsAction(employeeId: number) — calls getVacationRecords(employeeId), returns ActionResult<VacationRecord[]>
   - No revalidatePath (read-only)

2. getAllVacationRecordsAction() — calls getAllVacationRecords(), returns ActionResult<VacationRecord[]>
   - No revalidatePath (read-only)

3. addVacationRecordAction(input: Parameters<typeof addVacationRecord>[0]) — calls addVacationRecord(input), calls revalidatePath('/'), returns ActionResult<VacationRecord>

4. updateVacationRecordAction(input: Parameters<typeof updateVacationRecord>[0]) — calls updateVacationRecord(input), calls revalidatePath('/'), returns ActionResult<VacationRecord>

5. deleteVacationRecordAction(id: number) — calls deleteVacationRecord(id), calls revalidatePath('/'), returns ActionResult<void>

All functions: wrap in try/catch, return { success: false, error: e instanceof Error ? e.message : String(e) } on catch. Do NOT throw.

All input types are plain objects / primitives (dates as ISO strings, not Date instances).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>actions/vacationRecords.ts compiles without TypeScript errors; all 5 actions exported; 'use server' directive is first line; revalidatePath called in all mutation actions (add, update, delete); read actions do not call revalidatePath.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- File starts with `'use server';`
- revalidatePath('/') called in addVacationRecordAction, updateVacationRecordAction, deleteVacationRecordAction
- getVacationRecordsAction and getAllVacationRecordsAction do NOT call revalidatePath
- All 5 functions exported
- Import line references '@/lib/services/vacationRecords' (not a re-export from index)
</verification>

<success_criteria>
- src/lib/actions/vacationRecords.ts: 5 Server Actions wrapping vacation record services
- Discriminated union return type on all actions
- No thrown exceptions — errors returned as { success: false, error }
</success_criteria>

<output>
After completion, create `.planning/phases/02-schema-services-and-actions/02-06-SUMMARY.md`
</output>

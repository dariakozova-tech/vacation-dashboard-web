---
phase: 02-schema-services-and-actions
plan: "05"
type: execute
wave: 3
depends_on: ["02-04"]
files_modified:
  - drizzle/meta/_journal.json
autonomous: false
requirements:
  - INFRA-05

must_haves:
  truths:
    - "Drizzle schema is pushed to the live Neon database"
    - "employees and vacation_records tables exist in Neon"
    - "Seed data is inserted and employee count matches expected"
  artifacts:
    - path: "drizzle/meta/_journal.json"
      provides: "Migration journal showing applied schema"
      contains: "entries"
  key_links:
    - from: "drizzle.config.ts"
      to: "process.env.DATABASE_URL_UNPOOLED"
      via: "dbCredentials.url"
      pattern: "DATABASE_URL_UNPOOLED"
---

<objective>
Push the Drizzle schema to the live Neon database and run the seed script to populate it with all employees and vacation records.

Purpose: Completes the database layer. After this plan, the DB has tables and data that service functions can query in Phase 3.
Output: Tables created in Neon; _journal.json updated; seed data inserted.
</objective>

<execution_context>
@/Users/dariakozova/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dariakozova/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-schema-services-and-actions/02-RESEARCH.md
@.planning/phases/02-schema-services-and-actions/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push schema to Neon with drizzle-kit</name>
  <files>drizzle/meta/_journal.json</files>
  <action>
Run:
```bash
npx drizzle-kit push
```

This applies the schema.ts definitions to the live Neon database using DATABASE_URL_UNPOOLED from .env.local. The drizzle.config.ts already references the correct env var and schema path.

Expected outcome: drizzle-kit creates the record_type enum, employees table, and vacation_records table. The _journal.json will be updated with applied entries.

If drizzle-kit prompts about dropping/renaming columns (stale state), accept changes since this is a fresh database with no existing tables.

Do NOT use DATABASE_URL (pooled) — drizzle-kit requires the unpooled connection string for DDL operations.
  </action>
  <verify>
    <automated>npx drizzle-kit push 2>&1 | tail -5</automated>
  </verify>
  <done>drizzle-kit push exits 0; employees and vacation_records tables confirmed created in Neon.</done>
</task>

<task type="auto">
  <name>Task 2: Run seed script</name>
  <files><!-- No file output — this task executes src/scripts/seed.ts which writes rows to the Neon DB, not to disk --></files>
  <action>
Run the seed script:
```bash
npx tsx src/scripts/seed.ts
```

Expected output: "Seeded N employees, M vacation records" where N matches the Electron app employee count and M matches the total record count.

If output is "Already seeded", that means the idempotency guard triggered — this is acceptable if data was already inserted.

The seed script uses DATABASE_URL from .env.local (not UNPOOLED — this is a query, not DDL).
  </action>
  <verify>
    <automated>npx tsx src/scripts/seed.ts 2>&1 | tail -5</automated>
  </verify>
  <done>Seed script exits 0; employees and vacation records are present in Neon (confirmed by log output).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full database layer: schema pushed to Neon, seed data inserted. employees and vacation_records tables should exist with all ТОВ Текері employees and their vacation history.</what-built>
  <how-to-verify>
    1. Open Neon console (console.neon.tech)
    2. Navigate to the vacation-dashboard-web database
    3. Run: SELECT COUNT(*) FROM employees; — expect 5+ rows
    4. Run: SELECT COUNT(*) FROM vacation_records; — expect 20+ rows
    5. Run: SELECT full_name, hire_date FROM employees ORDER BY full_name; — verify Cyrillic names and hire dates match Electron app
    6. Check that Бондаренко Олексій has a balance_reset record and Мельник Дмитро has one too
  </how-to-verify>
  <resume-signal>Type "approved" or describe any discrepancies found</resume-signal>
</task>

</tasks>

<verification>
- `npx drizzle-kit push` exits 0
- `npx tsx src/scripts/seed.ts` exits 0 (or "Already seeded")
- Human confirmed: tables exist with expected row counts and correct data
</verification>

<success_criteria>
- Neon has employees and vacation_records tables
- Schema matches Drizzle definitions (column names, types, FK)
- All seed employees present with correct hire_date, is_deel, email values
- balance_reset records exist for Бондаренко and Мельник
</success_criteria>

<output>
After completion, create `.planning/phases/02-schema-services-and-actions/02-05-SUMMARY.md`
</output>

---
phase: 02-schema-services-and-actions
plan: 05
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - src/scripts/seed.ts
autonomous: true
requirements: [DB-03]
must_haves:
  truths:
    - "Seed script inserts all 42 employees (36 ТОВ + 6 Deel) into the employees table"
    - "Seed script inserts days_sum records for 2024 and 2025 for each employee that has non-zero values"
    - "Seed script inserts balance_reset records for employees where balance_reset: true"
    - "Seed script inserts all ARCHIVE_PERIODS (pre-2026 period records)"
    - "Seed script inserts all VACATION_2026 and DEEL_VACATIONS_2026 period records"
    - "Seed script is idempotent — running it twice does not insert duplicates"
    - "Script exits 0 and logs counts of inserted records"
  artifacts:
    - path: "src/scripts/seed.ts"
      provides: "One-time idempotent seed script for all employees and vacation records"
      min_lines: 100
  key_links:
    - from: "src/scripts/seed.ts"
      to: "src/lib/db/index.ts"
      via: "import { db } from '../lib/db'"
      pattern: "from '../lib/db'"
    - from: "src/scripts/seed.ts"
      to: "src/lib/db/schema.ts"
      via: "import { employees, vacationRecords } from '../lib/db/schema'"
      pattern: "from '../lib/db/schema'"
---

<objective>
Create the idempotent seed script that populates Neon with all employees and historical vacation records from the Electron app.

Purpose: The web app needs production data to be usable. This is a one-time operation run manually via `npx tsx src/scripts/seed.ts`. The script mirrors the Electron app's initialize() function behavior exactly.

Output: src/scripts/seed.ts — runnable script that inserts all seed data or skips if already seeded.
</objective>

<execution_context>
@/Users/dariakozova/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dariakozova/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@phases/02-schema-services-and-actions/02-02-SUMMARY.md
@phases/02-schema-services-and-actions/02-RESEARCH.md

<interfaces>
From src/lib/db/schema.ts (Plan 02 output):

```typescript
// employees table key columns for insert:
{
  fullName: string;   // maps to 'full_name'
  hireDate: string;   // 'YYYY-MM-DD', maps to 'hire_date'
  isActive?: boolean; // default true
  isDeel?: boolean;   // default false
  email?: string | null;
}

// vacationRecords table key columns for insert:
{
  employeeId: number;                               // FK
  recordType: 'period' | 'days_sum' | 'balance_reset';
  startDate?: string | null;   // 'YYYY-MM-DD'
  endDate?: string | null;     // 'YYYY-MM-DD'
  daysCount?: number | null;
  year?: number | null;
  note?: string | null;
}
```

Seed data source: /Users/dariakozova/Documents/vacation-dashboard/src/db/db.js

Employee arrays to seed:
- EMPLOYEES (36 ТОВ employees) — isDeel: false
- DEEL_CONTRACTORS (6 Deel employees) — isDeel: true

For each employee, insert vacation records:
- If used_2024 > 0: insert { recordType: 'days_sum', year: 2024, daysCount: used_2024, note: 'Archived 2024' }
- If used_2025 > 0: insert { recordType: 'days_sum', year: 2025, daysCount: used_2025, note: 'Archived 2025' }
- If balance_reset: true: insert { recordType: 'balance_reset', year: 2026, note: 'Balance reset 01.01.2026' }

ARCHIVE_PERIODS: ~90 period records — only insert for employees that exist in the seeded set (skip names not in ALLOWED_EMPLOYEES, e.g., Молчанов, Шаварин who are not in the main list)

VACATION_2026: 22 period records — same skip logic
DEEL_VACATIONS_2026: 3 period records

Idempotency strategy (mirrors Electron initialize()):
- Check: SELECT COUNT(*) FROM employees — if > 0, skip employee seeding
- For ARCHIVE_PERIODS and VACATION_2026: deduplicate on (employee_id, start_date) — use INSERT ... ON CONFLICT or SELECT before INSERT
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create seed script with employee and days_sum seeding</name>
  <files>src/scripts/seed.ts</files>
  <action>
    Create src/scripts/seed.ts. The script uses `dotenv/config` to load .env.local and imports db from '../lib/db'.

    Structure:
    1. Import `import 'dotenv/config'` at top (dotenv is already installed as devDependency)
    2. Import db and schema tables
    3. `async function main()` with all seeding logic
    4. `main().catch(console.error)`

    Idempotency: Check employee count first. If > 0, log "Already seeded, skipping employees" but still run vacation record syncs (idempotent via start_date check).

    Employee seeding:
    - Insert all EMPLOYEES with isDeel: false
    - Insert all DEEL_CONTRACTORS with isDeel: true
    - After insertion, build a Map<string, number> of fullName → id for use in record seeding

    days_sum and balance_reset record seeding per employee:
    - For each employee in EMPLOYEES/DEEL_CONTRACTORS:
      - If used_2024 > 0: insert { employeeId, recordType: 'days_sum', year: 2024, daysCount: used_2024, note: 'Архів 2024' } (ТОВ) or 'Archive 2024' (Deel)
      - If used_2025 > 0: same for 2025
      - If balance_reset: insert { employeeId, recordType: 'balance_reset', year: 2026, daysCount: null, note: 'Обнулення балансу 01.01.2026' }

    Include the full EMPLOYEES, DEEL_CONTRACTORS arrays verbatim from db.js (copy them into the seed script). They are plain data — no business logic dependency.

    Log counts: "Inserted N employees", "Inserted N records".
  </action>
  <verify>
    <automated>cd /Users/dariakozova/Documents/vacation-dashboard-web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles without errors. The employee data arrays and insertion logic are present in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Add archive periods and 2026 vacations to seed script, then run it</name>
  <files>src/scripts/seed.ts</files>
  <action>
    Extend seed.ts to include:

    ARCHIVE_PERIODS seeding (period records for 2024-2025):
    - Include the ARCHIVE_PERIODS array verbatim from db.js (~90 entries)
    - For each period: look up employeeId by fullName in the name→id map
    - Skip if fullName not in map (handles ex-employees like Молчанов, Шаварин who are not in the seeded employee list)
    - Idempotent insert: check `SELECT id FROM vacation_records WHERE employee_id = ? AND start_date = ?` before inserting
    - Insert: { employeeId, recordType: 'period', startDate: start, endDate: end, daysCount: days, year: start.substring(0,4) parsed as int, note: 'Архів' }

    VACATION_2026 seeding (ТОВ 2026 periods):
    - Include VACATION_2026 array verbatim (~22 entries)
    - Same skip + deduplicate logic as archive periods
    - note: 'Відпустка 2026'

    DEEL_VACATIONS_2026 seeding (3 Deel 2026 periods):
    - Include DEEL_VACATIONS_2026 array verbatim (3 entries)
    - Same logic, note: 'Vacation 2026'

    After completing the script, run it:
    ```bash
    npx tsx src/scripts/seed.ts
    ```

    tsx is available as it's a standard companion to TypeScript — if not installed, run `npm install --save-dev tsx` first, then seed.
  </action>
  <verify>
    <automated>cd /Users/dariakozova/Documents/vacation-dashboard-web && npx tsx src/scripts/seed.ts 2>&1</automated>
  </verify>
  <done>Script exits 0 and logs inserted counts. On a second run, it logs "Already seeded, skipping employees" and exits 0 with 0 new records inserted (idempotent).</done>
</task>

</tasks>

<verification>
- `npx tsx src/scripts/seed.ts` exits 0 on first run with employee + record counts logged
- `npx tsx src/scripts/seed.ts` exits 0 on second run with "already seeded" message and 0 inserts
- `npx tsc --noEmit` exits 0
- Script does NOT run at app startup — it is standalone only
</verification>

<success_criteria>
All 42 employees and their complete vacation history (days_sum, balance_reset, archive periods, 2026 periods) are in the Neon database. Script is idempotent and safe to re-run.
</success_criteria>

<output>
After completion, create `phases/02-schema-services-and-actions/02-05-SUMMARY.md`
</output>
